// Popup entry. Currently popup.html still loads /popup.js (the consolidated
// 4900-line file at the repo root). Pending Phase 7b: popup.html updates to
// <script type="module" src="src/popup/main.js"></script> and main.js imports
// from the per-render submodules under render/, exports/, blue/.
export {};
