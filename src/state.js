// state{}, T(), seen() with LRU, _seen Maps, _debugTabs Set, _pending,
// _scripts, _loadTimers Map, markDirty, flushDirty, serializeTabState,
// restoreStateOnStartup. Currently still inline at lines ~32-360 of
// src/background.js. Pending Phase 7b extraction.
export {};
